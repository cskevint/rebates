h1 Territories

.sub-nav
  = link_to 'New Territory', new_zone_path

table(cellspacing=0)
  tr
    th Name
    th Rebates
    th

  - @zones.each do |zone|
    tr
      td =link_to  zone.name, zone
      td = link_to 'Rebates', zone_rebates_path(zone)
      td
        = link_to 'Edit', edit_zone_path(zone)
        '|
        = link_to 'Remove', zone, :confirm => 'Are you sure?', :method => :delete

